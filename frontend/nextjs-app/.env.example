# `npm run dev` 시 백오피스(/backoffice) 헬스 프록시가 게이트웨이로 붙는 주소
# 이 파일을 .env.local 로 복사한 뒤 포트가 다르면 수정하세요.
GATEWAY_INTERNAL_URL=http://localhost:8085

# 브라우저용 API 베이스(기존 앱과 동일)
NEXT_PUBLIC_API_URL=http://localhost:8085
