# cp nginx/.env.nginx.example nginx/.env.nginx
# ./scripts/render-nginx.sh  → nginx/msa-project.rendered.conf (호스트 Nginx 전용, .gitignore)
#
# Docker 운영은 reverse-proxy 컨테이너가 루트 .env 의 PRIMARY_SERVER_NAMES·SSL_* 등을 읽습니다.

NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

PRIMARY_SERVER_NAMES="example.com www.example.com"

SSL_CERT_PATH=/etc/letsencrypt/live/example.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/example.com/privkey.pem

# 호스트 Nginx → 127.0.0.1 의 compose published 포트
UPSTREAM_GATEWAY=127.0.0.1:8085
UPSTREAM_FRONTEND=127.0.0.1:3000

# /metrics/node — 호스트: node_exporter 리슨 주소. Docker 내 Nginx: node-exporter:9100
NODE_EXPORTER_UPSTREAM=127.0.0.1:9100

METRICS_ALLOW_IP=127.0.0.1

# 개발 서버 블록 (미사용 시 NGINX_ENABLE_DEV_SERVER=0 유지)
NGINX_ENABLE_DEV_SERVER=0
DEV_DOMAIN=dev.example.com
DEV_SSL_CERT=/etc/letsencrypt/live/dev.example.com/fullchain.pem
DEV_SSL_KEY=/etc/letsencrypt/live/dev.example.com/privkey.pem
DEV_UPSTREAM_FRONTEND=127.0.0.1:4000
DEV_UPSTREAM_GATEWAY=127.0.0.1:9085
